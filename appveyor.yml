# appveyor.yml
image:
  - Visual Studio 2017

init:
  - cmd: set QT_DIR=C:\Qt\5.12.2\msvc2017
  - cmd: set PATH=%QT_DIR%\bin;%PATH%
  - cmd: qmake --version
  
build_script:
  - cmd: echo %PATH%
  - cmd: qmake
  - cmd: nmake
  - cmd: cd output
  - cmd: windeployqt serialport-tools.exe
  - cmd: dir
  
  